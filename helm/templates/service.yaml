apiVersion: v1
kind: Service
metadata:
  name: {{ include "chabloom-ecommerce-backend.fullname" . }}
  labels:
    {{- include "chabloom-ecommerce-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chabloom-ecommerce-backend.selectorLabels" . | nindent 4 }}
