apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Chabloom.Ecommerce.Backend.fullname" . }}-test-connection"
  labels:
    {{- include "Chabloom.Ecommerce.Backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Chabloom.Ecommerce.Backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
