trigger:
  - master

pool:
  vmImage: "ubuntu-latest"

variables:
  buildConfiguration: "Release"

steps:
  - task: Docker@2
    inputs:
      containerRegistry: 'docker-registry-chb-dev-1'
      repository: 'chabloom-ecommerce-backend'
      command: 'buildAndPush'
      Dockerfile: '**/Dockerfile'
      tags: |
        $(Build.BuildId)
        latest
