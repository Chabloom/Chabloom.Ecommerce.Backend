trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'Docker Hub'
    repository: 'mdcasey/chabloom-ecommerce-backend'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
